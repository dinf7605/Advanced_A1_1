# 📄 프롬프트 관리 프로그램 PRD v2 & 7일 실행 계획

> AI 프롬프트를 저장·조회·검색·즐겨찾기할 수 있는 Python 콘솔 프로그램 개발 계획서

| 항목 | 내용 |
|------|------|
| 문서 버전 | v2 (2026-08-11) |
| 이전 버전 | `prompt_manager_PRD.txt` (v1) |

### v2 주요 변경점

| # | 변경 내용 | 이유 |
|---|-----------|------|
| 1 | 병합 시 `--no-ff` 강제 | fast-forward 되면 `--graph`에 병합 흔적이 안 남음 |
| 2 | `git pull` 실습 시나리오 구체화 | 1인 개발은 pull이 "Already up to date"로 끝나 기록이 안 남음 |
| 3 | `git clone` 증적(스크린샷) 선행 규칙 | 삭제하면 사용 근거가 사라짐 |
| 4 | 기본 프롬프트 3개 실물 확정 슬롯 추가 | 채점 항목("이전 미션 프롬프트 3개 이상") |
| 5 | 번호 체계 규칙 신설 | 필터 화면에서 번호 재부여 시 오작동 |
| 6 | 메뉴 번호 확정 | 코드·README·스크린샷 일관성 |
| 7 | 예외 처리 명세표 신설 | 요구사항의 "안내 메시지" 조건 6종 누락 방지 |
| 8 | 함수 시그니처/데이터 전달 규약 명시 | 코드 품질(함수 분리) 채점 대응 |
| 9 | VSCode-GitHub 연동, Korean Pack 항목 추가 | v1 Day 1에서 누락 |
| 10 | 스크린샷 파일명 규칙 + 수동 테스트 시나리오 신설 | 최종일 누락 방지 |
| 11 | 커밋 메시지 사전 확정(19개) | 10개 기준 여유 확보 |
| 12 | Git 명령어 8종 설명 섹션 추가 | 과제 목표 "설명할 수 있다" 대응 |

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 이름 | Prompt Manager (프롬프트 관리 콘솔 프로그램) |
| 목적 | 자주 쓰는 AI 프롬프트를 저장·조회·검색·즐겨찾기하는 CLI 프로그램 |
| 환경 | Python 3.10+ / 외부 라이브러리 미사용(필수 기능) |
| 학습 목표 | Python 기초 문법 + Git/GitHub 워크플로우 체득 |
| 진행 방식 | 1인 개발 (혼자 진행) |
| 파일 구조 | `prompt_manager.py` **단일 파일** (필수 기능 전부 포함) |

### 실제 환경 확인 결과 (2026-08-11)

| 항목 | 상태 |
|------|------|
| Python | `3.14.6` ✅ (3.10 이상 충족) |
| Git | `2.55.0.windows.3` ✅ |
| Git 전역 설정 | ❌ **미설정** (`.gitconfig` 없음) → Day 1 최우선 작업 |
| 저장소 | ❌ 아직 `git init` 안 됨 |

---

## 2. 요구사항 추적표 (과제 요건 ↔ 대응 계획)

제출 직전 이 표만 보고 자체 채점할 수 있게 만든 표입니다.

| 과제 요건 | 대응 위치 | 완료 |
|-----------|-----------|------|
| 메뉴 출력 / 번호 선택 | `show_menu()`, `main()` | [ ] |
| 잘못된 번호 → 안내 후 메뉴 재출력 | §5 예외처리 E1 | [ ] |
| 종료 기능 | 메뉴 `0` | [ ] |
| 기능 수행 후 메뉴 복귀 | `main()` while 루프 | [ ] |
| 기본 프롬프트 3개 이상(이전 미션) | §4.1 | [ ] |
| 리스트+딕셔너리 저장 | §4.1 | [ ] |
| 제목/내용/카테고리/즐겨찾기 필드 | §4.1 | [ ] |
| 프롬프트 추가 (빈 입력 재요청) | `add_prompt()`, E2 | [ ] |
| 카테고리 선택 또는 직접 입력 | §4.3 | [ ] |
| 즐겨찾기 기본 False | `add_prompt()` | [ ] |
| **목록 보기 — 브랜치에서 작업 후 merge** | Day 4 | [ ] |
| 목록에 번호·제목·카테고리·⭐ 표시 | `show_list()` | [ ] |
| 프롬프트 없을 때 안내 | E4 | [ ] |
| 카테고리별 조회 + 없을 때 안내 | `show_by_category()`, E6 | [ ] |
| 검색(제목/내용) + 결과 없을 때 안내 | `search_prompt()`, E5 | [ ] |
| 상세 보기(전체 내용) + 번호 오류 안내 | `show_detail()`, E3 | [ ] |
| 즐겨찾기 추가/해제 | `toggle_favorite()` | [ ] |
| 즐겨찾기만 모아보기 | `show_favorites()` | [ ] |
| 기능별 함수 분리 | §3.2 | [ ] |
| 커밋 10개 이상 | §7 커밋 목록(19개 계획) | [ ] |
| init/add/commit/push/pull/checkout/clone/merge 각 1회+ | §8 | [ ] |
| 브랜치 생성 및 병합 기록 | Day 4 (`--no-ff`) | [ ] |
| README (설명/실행방법/기능목록/카테고리) | Day 6 | [ ] |
| .gitignore | Day 1 | [ ] |
| 스크린샷 3종 | §9 | [ ] |

---

## 3. 기능 명세

### 3.1 메뉴 번호 (확정)

```
========================================
        📌 프롬프트 관리 프로그램
========================================
 1. 프롬프트 추가
 2. 프롬프트 목록 보기
 3. 카테고리별 조회
 4. 프롬프트 검색
 5. 프롬프트 상세 보기
 6. 즐겨찾기 추가/해제
 7. 즐겨찾기 목록 보기
 0. 종료
========================================
번호를 선택하세요:
```

> 이 번호는 코드·README·스크린샷에서 **끝까지 동일하게** 유지한다.

### 3.2 함수 목록 및 데이터 전달 규약

**규약: 전역 변수를 쓰지 않고 `prompts` 리스트를 인자로 전달한다.**
(리스트는 가변 객체라 함수 안에서 수정하면 원본에 반영된다 — `return` 불필요)

```python
CATEGORIES = ["텍스트 생성", "이미지 생성", "영상 생성", "페르소나", "자동화", "기타"]

def get_default_prompts():          # 기본 프롬프트 3개 이상 반환
def show_menu():                    # 메뉴 출력
def add_prompt(prompts):            # 제목/내용/카테고리 입력받아 추가
def show_list(prompts):             # 전체 목록 출력
def show_by_category(prompts):      # 카테고리 선택 → 필터 출력
def search_prompt(prompts):         # 키워드 검색
def show_detail(prompts):           # 번호 입력 → 전체 내용 출력
def toggle_favorite(prompts):       # 번호 입력 → 즐겨찾기 on/off
def show_favorites(prompts):        # 즐겨찾기만 출력
def get_categories(prompts):        # [공통] 카테고리 목록 (상수 ∪ 데이터, §4.3)
def print_prompt_line(number, p):   # [공통] 목록 한 줄 출력 (Day 6 리팩토링)
def input_number(prompts):          # [공통] 번호 입력+검증 (Day 6 리팩토링)
def main():                         # 메인 루프
```

> `print_prompt_line()`, `input_number()`는 Day 6 리팩토링 커밋에서 중복 제거용으로 도입한다.
> "중복을 함수로 뽑아냈다"는 것 자체가 코드 품질 채점 근거가 된다.

---

## 4. 데이터 구조 및 규칙

### 4.1 기본 프롬프트 (확정 — 6개)

**출처: 이전 미션 「PRD 초안 작성 자동화 — LLM 업무 자동화 과제」(`프롬프트1.md`)에서 실제 사용한 프롬프트.**
시스템 프롬프트 / 유저 프롬프트 / 입력 템플릿 / 목업 생성 프롬프트 / 환각 검증 질문을 원문 그대로 옮겼다.

| # | 제목 | 카테고리 |
|---|------|----------|
| 1 | PRD 도우미 '진' 시스템 프롬프트 (v2) | 페르소나 |
| 2 | PRD 초안 요청 유저 프롬프트 | 자동화 |
| 3 | PRD 입력 템플릿 (바코드 스캔 재고 등록) | 자동화 |
| 4 | 입고 등록 화면 목업 생성 | 이미지 생성 |
| 5 | 환각 검증 질문 세트 (5문항) | 기타 |
| 6 | PRD 도우미 시스템 프롬프트 (v1, 개선 전) | 페르소나 |

> **이 구성의 장점**
> - 카테고리가 4종(페르소나·자동화·이미지 생성·기타)으로 흩어져 "카테고리별 조회" 데모가 살아난다.
> - `텍스트 생성`·`영상 생성`은 비어 있어 **E6(해당 카테고리 없음) 안내 메시지 스크린샷**을 찍을 수 있다. (§4.3 방식 적용 시)
> - 1번은 내용이 길어 "상세 보기"가 왜 필요한지 드러나고, 6번(v1)과 비교하면 프롬프트 개선 이력도 보인다.
> - 검색 데모: `PRD` → 5건(1·2·3·5·6), `바코드` → 2건(3·4). 실측 확인 완료.

```python
def get_default_prompts():
    """이전 미션(PRD 초안 작성 자동화)에서 작성한 프롬프트를 기본 데이터로 등록한다."""
    return [
        {
            "title": "PRD 도우미 '진' 시스템 프롬프트 (v2)",
            "content": (
                "당신은 'PRD 도우미 진'이며, 10년차 시니어 프로덕트 매니저다.\n"
                "목표: 사용자가 제공한 입력 템플릿을 바탕으로 표준 형식의 PRD 초안을 작성한다.\n"
                "\n"
                "[출력 형식 규칙]\n"
                "- 다음 목차를 반드시 사용: 1)목적 2)대상 사용자 3)핵심 시나리오 "
                "4)기능 요구사항 5)수용 기준(AC) 6)리스크·미결정\n"
                "- 각 항목은 불릿으로, 수용 기준은 'Given/When/Then' 형식.\n"
                "- 톤은 간결한 실무형. 존댓말.\n"
                "\n"
                "[안전장치]\n"
                "- 입력 템플릿에 비어 있거나 모호한 항목이 있으면, 초안을 쓰기 전에 "
                "먼저 확인 질문을 한다. (임의로 채우지 않는다)\n"
                "- 사실·수치·정책이 필요한 부분에서 확실하지 않으면 "
                "'확인 필요: (무엇을 어디서 확인)'으로 표기한다.\n"
                "- 모르는 것은 모른다고 말한다.\n"
                "\n"
                "[추론 규칙]\n"
                "- 내부적으로 (요구사항 정리 → 누락 항목 점검 → 초안) 순서로 단계적으로 "
                "검토하되, 그 과정을 장문으로 노출하지 않는다.\n"
                "- 최종 답변에는 초안 본문과, 판단 근거를 핵심 3개 불릿으로만 제시한다.\n"
                "\n"
                "[금지] 확인 안 된 정보를 단정, 요구사항 창작, 금지어 사용."
            ),
            "category": "페르소나",
            "favorite": False,
        },
        {
            "title": "PRD 초안 요청 유저 프롬프트",
            "content": (
                "아래 입력 템플릿을 바탕으로 PRD 초안을 작성해 주세요.\n"
                "비어 있거나 모호한 항목이 있으면 먼저 확인 질문을 해주세요.\n"
                "\n"
                "--- 입력 템플릿 시작 ---\n"
                "[기능명] ...\n"
                "[목적] ...\n"
                "[대상 사용자] ...\n"
                "--- 입력 템플릿 끝 ---"
            ),
            "category": "자동화",
            "favorite": False,
        },
        {
            "title": "PRD 입력 템플릿 (바코드 스캔 재고 등록)",
            "content": (
                "[기능명] 바코드 스캔 재고 등록\n"
                "[목적] 입고 시 수기 입력 오류 감소\n"
                "[대상 사용자] 창고 입고 담당자 (입고 시 상시 사용)\n"
                "[핵심 사용 시나리오] 스캔 → 상품정보 자동입력 → 수량 입력 → 등록\n"
                "[성공 지표] 입고 등록 오류율 5% → 1%\n"
                "[제약/정책] 전용 바코드 스캐너 사용\n"
                "[톤] 간결·실무형\n"
                "[금지어] '완벽하게', '100% 보장'\n"
                "[필수 포함 항목] 목적/사용자/기능요구/수용기준/리스크"
            ),
            "category": "자동화",
            "favorite": False,
        },
        {
            "title": "입고 등록 화면 목업 생성",
            "content": (
                "바코드 스캔 재고 등록 앱의 화면 목업을 그려줘.\n"
                "전용 스캐너로 바코드를 스캔하면 상품명·SKU가 자동으로 채워지는 "
                "입고 등록 화면. 오프라인 저장 상태 표시 포함.\n"
                "깔끔한 실무용 UI, 한국어 인터페이스."
            ),
            "category": "이미지 생성",
            "favorite": False,
        },
        {
            "title": "환각 검증 질문 세트 (5문항)",
            "content": (
                "1) PRD에서 'AC'는 무슨 약자야?\n"
                "2) 이 기능의 성공 지표 목표치는 몇 %야?\n"
                "3) GDPR상 개인정보 보관 최대 기간은 며칠이야?\n"
                "4) MoSCoW 우선순위 기법의 4개 등급을 말해줘.\n"
                "5) 우리 회사 배포 정책상 금요일 배포가 금지야?\n"
                "\n"
                "※ 2·3·5번은 임의 단정을 유도하는 함정 질문. '확인 필요' 응답이 정답."
            ),
            "category": "기타",
            "favorite": False,
        },
        {
            "title": "PRD 도우미 시스템 프롬프트 (v1, 개선 전)",
            "content": (
                "당신은 PM입니다. 아래 입력으로 PRD 초안을 작성해 주세요.\n"
                "목차: 목적/대상/기능/수용기준/리스크.\n"
                "\n"
                "※ v1의 문제: 모호한 입력에도 되묻지 않고 값을 지어냄, "
                "정책 수치를 근거 없이 단정, 출력 형식이 매번 흔들림. → v2에서 개선."
            ),
            "category": "페르소나",
            "favorite": False,
        },
    ]
```

### 4.2 번호 체계 규칙 (중요)

> **모든 화면에서 "전체 리스트 기준 번호(index + 1)"만 사용한다.**

검색·카테고리·즐겨찾기 결과에서 번호를 `1, 2, 3...`으로 다시 매기면,
사용자가 그 번호로 상세 보기나 즐겨찾기를 시도할 때 **엉뚱한 프롬프트가 선택된다.**

```python
# ❌ 잘못된 방식 — 필터 결과에 번호를 새로 매김
for i, p in enumerate(results, 1):
    print(f"{i}. {p['title']}")

# ✅ 올바른 방식 — 원래 인덱스를 함께 들고 다님
for i, p in enumerate(prompts, 1):
    if keyword in p["title"] or keyword in p["content"]:
        print(f"{i}. {p['title']}")
```

### 4.3 카테고리 규칙

- 추가 시: `CATEGORIES` 목록을 번호와 함께 보여주고 **번호 선택 또는 직접 입력** 모두 허용
- 빈 값 입력 시 → `"기타"`로 처리하거나 재입력 요청 (택1하여 일관 적용)
- **카테고리별 조회 목록 = `CATEGORIES` 상수 ∪ 데이터에서 추출한 카테고리 (합집합)**

**왜 합집합인가**

| 방식 | 문제 |
|------|------|
| 상수만 사용 | 사용자가 직접 입력한 카테고리를 조회할 수 없다 |
| 데이터에서만 추출 | 비어 있는 카테고리가 목록에 안 떠서 **E6(해당 카테고리 없음) 안내를 띄울 방법이 없다** |
| **합집합** ✅ | 둘 다 해결 |

```python
def get_categories(prompts):
    categories = list(CATEGORIES)              # 미리 정의된 6개
    for p in prompts:
        if p["category"] not in categories:
            categories.append(p["category"])   # 직접 입력한 카테고리 추가
    return categories
```

> 기본 데이터(§4.1) 기준으로 `텍스트 생성`·`영상 생성`이 비어 있으므로,
> 이 둘 중 하나를 고르면 E6 안내가 뜬다 → `09_category.png` 스크린샷 소재.

### 4.4 즐겨찾기 표시

- 표기: 즐겨찾기 O → `⭐`, X → 공백 또는 `-`
- ✅ **인코딩 확인 완료 (2026-08-11 / Python 3.14.6 / 본 PC)**
  `python -c "print('⭐ 상태 확인')"` → 파이프 리다이렉션 상황에서도 정상 출력됨.
  → **`⭐`를 그대로 사용해도 된다.** 폴백(`[★]`, `[*]`) 불필요.
- 단, VSCode 터미널 폰트에 따라 네모로 보일 수 있으므로 **스크린샷 찍기 전 눈으로 한 번 확인**할 것.

---

## 5. 예외 처리 명세

| ID | 상황 | 처리 |
|----|------|------|
| E1 | 메뉴에 숫자 아닌 값 / 범위 밖 번호 / 빈 입력 | `"잘못된 번호입니다. 다시 선택해주세요."` 출력 후 메뉴 재출력 |
| E2 | 프롬프트 추가 시 제목 또는 내용이 공백 | `"제목을 입력해주세요."` 출력 후 **재입력 요청(while 루프)** |
| E3 | 상세 보기 / 즐겨찾기에서 잘못된 번호 | `"해당 번호의 프롬프트가 없습니다."` 출력 후 메뉴 복귀 |
| E4 | 목록 조회 시 프롬프트 0개 | `"등록된 프롬프트가 없습니다."` |
| E5 | 검색 결과 0건 | `"'{키워드}'에 대한 검색 결과가 없습니다."` |
| E6 | 선택한 카테고리에 프롬프트 0건 | `"해당 카테고리에 등록된 프롬프트가 없습니다."` |
| E7 | 즐겨찾기 0건 | `"즐겨찾기한 프롬프트가 없습니다."` |

**입력값은 항상 `.strip()` 처리한다** (공백만 입력한 경우를 빈 값으로 간주).

---

## 6. 화면 설계 예시

```
[목록 보기]
번호  제목                    카테고리        즐겨찾기
--------------------------------------------------------
 1   블로그 글 초안 작성        텍스트 생성        ⭐
 2   제품 사진 이미지 생성      이미지 생성
 3   면접관 페르소나 설정       페르소나

[상세 보기]
========================================
 [3] 면접관 페르소나 설정
 카테고리 : 페르소나
 즐겨찾기 : 아니오
----------------------------------------
 너는 IT 기업의 시니어 개발자 면접관이야. ...
========================================
```

---

## 7. 7일 실행 계획 v2

> 시작일 2026-08-11(화) 기준. 실제 마감일에 맞춰 날짜를 조정할 것.

### Day 1 — 8/11(화) 환경 세팅 & 저장소 초기화

- [ ] VSCode에 **Python 확장** 설치
- [ ] VSCode에 **Korean Language Pack** 설치 (선택)
- [ ] `python --version` 확인 (3.10+) → ✅ 3.14.6 확인됨
- [ ] `hello.py`에 `print("Hello")` 작성 후 실행
- [ ] `git --version` 확인 → ✅ 2.55.0 확인됨
- [ ] **Git 설정 3종** (현재 미설정 상태!)
  - `git config --global user.name "이름"`
  - `git config --global user.email "이메일"`
  - `git config --global init.defaultBranch main`
- [ ] **VSCode에서 GitHub 계정 로그인 및 연동 확인** ← v1 누락 항목
- [ ] GitHub 새 저장소 생성 (README 체크 해제)
- [ ] 로컬 `git init` → `git remote add origin` → 첫 `add`/`commit`/`push`
- [ ] `.gitignore` 작성 (§8.2)
- [ ] `README.md` 생성 (제목만)
- [ ] **공개 저장소 `git clone` → 폴더 구조 + `git log` 확인 → 📸 스크린샷 먼저 → 그 후 삭제**

📸 **스크린샷**: `01_env_python.png`, `02_git_config.png`, `03_vscode_github.png`, `04_clone.png`

**커밋**
1. `chore: 프로젝트 초기 세팅 및 .gitignore 추가`
2. `docs: README 초안 작성`

---

### Day 2 — 8/12(수) 기본 데이터 & 메뉴 골격

- [ ] `get_default_prompts()` — 이전 미션 프롬프트 3개 이상 등록
- [ ] `⭐` 출력 테스트 (§4.4)
- [ ] `show_menu()` 작성 (§3.1 번호 확정본)
- [ ] `main()` — `while True` 루프 + 메뉴 복귀
- [ ] 잘못된 입력 처리(E1), 종료(`0`)
- 💡 나머지 기능은 `"준비 중입니다"` 출력만 해도 OK

**커밋**
3. `feat: 기본 프롬프트 데이터 3개 등록`
4. `feat: 메뉴 출력 및 메인 루프 구현`
5. `feat: 잘못된 입력 처리 및 종료 기능 구현`

---

### Day 3 — 8/13(목) 추가 / 상세 보기

- [ ] `add_prompt()` — 빈 입력 재요청(E2), 즐겨찾기 기본 `False`
- [ ] 카테고리 번호 선택 + 직접 입력 처리 (§4.3)
- [ ] `show_detail()` — 번호 유효성 검사(E3)

**커밋**
6. `feat: 프롬프트 추가 기능 구현`
7. `feat: 카테고리 선택 및 빈 입력 재요청 처리`
8. `feat: 프롬프트 상세 보기 기능 구현`

---

### Day 4 — 8/14(금) 목록 기능 ⭐ **브랜치 실습 (가장 중요)**

```bash
git checkout -b feature/list
# show_list() 구현 → 커밋 (2개)
git checkout main
git merge --no-ff feature/list -m "merge: 목록 보기 기능 병합"
git log --oneline --graph --all
```

- [ ] **`git checkout -b feature/list`로 브랜치 생성 후 그 안에서 작업**
- [ ] `show_list()` — 번호/제목/카테고리/⭐, 0개일 때 안내(E4)
- [ ] `git checkout main`으로 복귀
- [ ] **`git merge --no-ff`로 병합** ← `--no-ff` 없으면 fast-forward 되어 그래프에 갈래가 안 남는다
- [ ] `git log --oneline --graph --all`로 병합 갈래 눈으로 확인

**커밋**
9. `feat: 프롬프트 목록 보기 기능 구현` *(feature/list 브랜치)*
10. `style: 목록 출력 정렬 및 즐겨찾기 표시 추가` *(feature/list 브랜치)*
11. `merge: feature/list 브랜치 병합 (목록 보기 기능)` *(병합 커밋)*

---

### Day 5 — 8/15(토) 카테고리 / 검색 / 즐겨찾기

- [ ] `show_by_category()` — 등록된 데이터 기준 카테고리 목록(§4.3), 없을 때 안내(E6)
- [ ] `search_prompt()` — 제목 또는 내용 부분 일치, 없을 때 안내(E5)
- [ ] `toggle_favorite()` — 번호로 on/off, 잘못된 번호 안내(E3)
- [ ] `show_favorites()` — 없을 때 안내(E7)
- [ ] ⚠️ 모든 화면에서 §4.2 번호 규칙 준수 확인

**커밋**
12. `feat: 카테고리별 조회 기능 구현`
13. `feat: 프롬프트 검색 기능 구현`
14. `feat: 즐겨찾기 추가/해제 기능 구현`
15. `feat: 즐겨찾기 목록 보기 기능 구현`

---

### Day 6 — 8/16(일) 정리 & 문서화 & **pull 실습**

- [ ] §10 수동 테스트 시나리오 전체 실행
- [ ] 리팩토링: `print_prompt_line()`, `input_number()`로 중복 제거
- [ ] README 완성 (설명 / 실행 방법 / 기능 목록 / 카테고리 설명)
- [ ] **`git pull` 실습 — 반드시 아래 순서로**
  1. GitHub 웹에서 `README.md`를 직접 편집 → 커밋 (원격에만 존재하는 커밋 생성)
  2. 로컬에서 `git pull`
  3. 실제로 파일이 갱신되는 것을 확인 → 📸 스크린샷
  > 이 과정 없이 그냥 `git pull` 하면 `Already up to date`로 끝나 **사용 기록이 남지 않는다.**

📸 **스크린샷**: `05_menu.png`, `06_add.png`, `07_list.png`, `08_search.png`, `09_category.png`, `10_detail.png`, `11_favorite.png`, `12_error.png`, `13_pull.png`

**커밋**
16. `refactor: 목록 출력 및 번호 입력 로직 함수로 분리`
17. `fix: (발견된 버그) 수정`
18. `docs: README 완성 (실행 방법/기능 목록/카테고리)`

---

### Day 7 — 8/17(월) 마무리 & 보너스(선택)

- [ ] §2 요구사항 추적표 전체 자체 채점
- [ ] §11 제출물 체크리스트 확인
- [ ] `git log --oneline --graph --all` 📸 `14_git_graph.png`
- [ ] 최종 `git push`
- [ ] (여유 시) 보너스 §12

**커밋**
19. `docs: 실행 결과 스크린샷 추가`

> **총 19개 커밋 계획** (기준 10개 대비 충분한 여유)

---

## 8. Git 실습 시나리오

### 8.1 8개 명령어 사용 시점 (각 1회 이상 필수)

| 명령어 | 사용 시점 | 증적 |
|--------|-----------|------|
| `init` | Day 1 저장소 시작 | 터미널 스크린샷 |
| `add` | 매 커밋 전 | git log |
| `commit` | 19회 | git log |
| `push` | Day 1 최초 + 매일 | GitHub 저장소 |
| `pull` | **Day 6 (원격 선편집 후)** | 스크린샷 `13_pull.png` |
| `checkout` | Day 4 (`-b feature/list`, `main`) | git log --graph |
| `clone` | **Day 1 (삭제 전 스크린샷 필수)** | 스크린샷 `04_clone.png` |
| `merge` | **Day 4 (`--no-ff`)** | 병합 커밋 + graph |

> 최신 Git은 `switch`/`restore`를 권장하지만, **과제가 `checkout`을 요구하므로 `checkout`을 그대로 사용한다.**

### 8.2 `.gitignore` 내용

```gitignore
# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# VSCode
.vscode/

# OS
Thumbs.db
desktop.ini
```

> 보너스 1(JSON 저장)을 구현할 경우 `prompts.json` 처리를 결정할 것.
> **권장: 추적한다**(gitignore에 넣지 않는다). 채점자가 데이터 파일을 확인할 수 있어야 하므로.

### 8.3 브랜치 push 여부

과제는 "브랜치 생성 및 병합을 **로컬에서** checkout/merge로 수행"을 요구하므로
`feature/list` 브랜치를 원격에 push할 필요는 없다.
단, GitHub에서도 확인받고 싶다면 `git push origin feature/list`를 추가로 실행해도 무방하다.
**병합 후 브랜치는 삭제하지 말 것** (`git branch -d` 금지) — 그래프에 라벨을 남겨두는 편이 안전하다.

---

## 9. 스크린샷 계획

**저장 위치**: `docs/screenshots/`

| 분류 | 파일명 | 내용 |
|------|--------|------|
| 환경 | `01_env_python.png` | `python --version` + VSCode 화면 |
| 환경 | `02_git_config.png` | `git --version` + `git config --list` (user.name/email/defaultBranch 3개가 한 화면에) |
| 환경 | `03_vscode_github.png` | VSCode GitHub 계정 로그인 상태 |
| 환경 | `04_clone.png` | `git clone` 명령 + 클론 폴더의 `git log` |
| 실행 | `05_menu.png` ~ `12_error.png` | 메뉴/추가/목록/검색/카테고리/상세/즐겨찾기/오류 |
| Git | `13_pull.png` | `git pull`로 원격 변경사항 받아오는 화면 |
| Git | `14_git_graph.png` | `git log --oneline --graph --all` (병합 갈래가 보여야 함) |

---

## 10. 수동 테스트 시나리오 (Day 6 실행)

각 항목이 곧 스크린샷 소재가 된다.

- [ ] T1. 메뉴에 `abc` 입력 → 안내 후 메뉴 재출력
- [ ] T2. 메뉴에 `99` 입력 → 안내 후 메뉴 재출력
- [ ] T3. 메뉴에 엔터만 입력 → 안내 후 메뉴 재출력
- [ ] T4. 프롬프트 추가에서 제목 공백 → 재입력 요청
- [ ] T5. 프롬프트 추가에서 내용 공백 → 재입력 요청
- [ ] T6. 프롬프트 추가 후 목록에 정상 반영 (실행 중 유지 확인)
- [ ] T7. 상세 보기에 `0` / `999` / `abc` 입력 → 안내
- [ ] T8. 없는 키워드로 검색 → 결과 없음 안내
- [ ] T9. 프롬프트 0개인 카테고리 선택 → 안내
- [ ] T10. 즐겨찾기 토글 → 목록에 ⭐ 반영 → 다시 토글 → 해제 확인
- [ ] T11. 즐겨찾기 0건 상태에서 즐겨찾기 목록 → 안내
- [ ] T12. **검색 결과에 뜬 번호로 상세 보기 → 같은 프롬프트가 나오는지 확인** (§4.2 검증)
- [ ] T13. 프로그램 재실행 → 추가했던 프롬프트가 사라짐 (필수 요건: 종료 시 초기화)

---

## 11. 제출물 체크리스트

- [ ] GitHub 저장소 URL
- [ ] 커밋 10개 이상 (계획 19개)
- [ ] **병합 커밋이 `--graph`에서 갈래로 보이는가** (`--no-ff` 확인)
- [ ] `init`/`add`/`commit`/`push`/`pull`/`checkout`/`clone`/`merge` 전부 사용 + 증적
- [ ] 스크린샷 3종(환경 / 실행 / git log graph) 전부 확보
- [ ] README 완성 (설명 / 실행 방법 / 기능 목록 / 카테고리)
- [x] **이전 미션 프롬프트 3개 이상 기본 등록** ← §4.1에 6개 확정 (`프롬프트1.md` 원문)
- [ ] **기능별 함수 분리** (한 함수에 몰아넣지 않음)
- [ ] **실행 중 추가/즐겨찾기 상태 유지, 종료 시 초기화** 동작 확인
- [ ] 외부 라이브러리 미사용 (필수 기능)

---

## 12. 보너스 과제 (선택)

### 보너스 1 – 영속화 및 내보내기
- JSON 파일 저장/불러오기 (`json` 모듈은 표준 라이브러리라 제약 위반 아님)
- 카테고리별 Markdown 파일 내보내기
- ⚠️ **주의**: JSON 로드가 기본 프롬프트 3개를 덮어써 사라지면 필수 요건이 깨진다.
  → **파일이 없을 때만 `get_default_prompts()`를 사용**하도록 분기할 것.

### 보너스 2 – CRUD 및 사용 기록
- 프롬프트 수정/삭제
- 상세 보기 시 조회수(`views`) 기록 → 딕셔너리에 필드 추가
- 조회수 기준 Top 목록 정렬 (`sorted(prompts, key=lambda p: p["views"], reverse=True)`)

---

## 13. 커밋 메시지 컨벤션

| 접두어 | 용도 |
|--------|------|
| `feat:` | 새 기능 추가 |
| `fix:` | 버그 수정 |
| `docs:` | 문서 수정 (README 등) |
| `refactor:` | 코드 리팩토링 (동작 변화 없음) |
| `style:` | 출력 포맷·들여쓰기 등 |
| `chore:` | 설정/기타 작업 |
| `merge:` | 브랜치 병합 |

**예시**: `feat: 프롬프트 검색 기능 구현`

---

## 14. Git 명령어 정리 (과제 목표 "설명할 수 있다" 대응)

> 이 내용은 README 하단이나 `NOTES.md`로 저장소에 포함시킨다. 문서 커밋도 하나 더 확보된다.

**Git이란?** 파일의 변경 이력을 시점별로 저장해두는 버전 관리 도구.
언제 무엇을 왜 바꿨는지 기록되고, 잘못되면 이전 시점으로 되돌릴 수 있으며,
브랜치로 여러 작업을 분리했다가 합칠 수 있다.

| 명령어 | 하는 일 |
|--------|---------|
| `init` | 현재 폴더를 Git 저장소로 만든다 (`.git` 폴더 생성) |
| `add` | 변경된 파일을 커밋 대상(스테이징 영역)에 올린다 |
| `commit` | 스테이징된 변경을 하나의 시점으로 저장소에 기록한다 |
| `push` | 로컬 커밋을 원격 저장소(GitHub)로 올린다 |
| `pull` | 원격 저장소의 변경사항을 로컬로 가져와 합친다 (`fetch` + `merge`) |
| `checkout` | 브랜치를 이동하거나 새로 만들며 이동한다 (`-b`) |
| `clone` | 원격 저장소 전체를 이력째 로컬로 복제한다 |
| `merge` | 다른 브랜치의 변경사항을 현재 브랜치로 합친다 |

**스테이징이 왜 있나?** 작업한 것 전부가 아니라 *이번 커밋에 포함할 것만* 골라 담기 위해서.
그래서 "기능 단위 커밋"이 가능해진다.

---

## 15. 리스크 & 대응

| 리스크 | 대응 |
|--------|------|
| merge가 fast-forward 되어 그래프에 안 보임 | `--no-ff` 필수 (Day 4) |
| pull이 `Already up to date`로 끝남 | 원격에서 README 먼저 수정 (Day 6) |
| clone 폴더 삭제로 증적 소실 | 삭제 **전** 스크린샷 (Day 1) |
| ~~⭐ 이모지가 콘솔에서 깨짐~~ | ✅ 해소 — 본 PC에서 정상 출력 확인 (§4.4) |
| 필터 화면 번호 불일치 | §4.2 규칙 준수, T12로 검증 |
| ~~기본 프롬프트가 이전 미션과 무관~~ | ✅ 해소 — §4.1에 실제 프롬프트 6개 확정 |
| Day 5 분량 과다(기능 4개) | 여유 없으면 카테고리 조회를 Day 3으로 앞당김 |
| 커밋 몰아치기(하루에 전부) | 날짜가 하루에 몰리면 "기능 단위 커밋" 신뢰도 하락 — 매일 조금씩 |

---

## 부록 A. 프로그램 뼈대 (Day 2 시작점)

```python
CATEGORIES = ["텍스트 생성", "이미지 생성", "영상 생성", "페르소나", "자동화", "기타"]


def get_default_prompts():
    return [
        # §4.1 참고 — 이전 미션 프롬프트로 교체
    ]


def show_menu():
    print("\n" + "=" * 40)
    print("        📌 프롬프트 관리 프로그램")
    print("=" * 40)
    print(" 1. 프롬프트 추가")
    print(" 2. 프롬프트 목록 보기")
    print(" 3. 카테고리별 조회")
    print(" 4. 프롬프트 검색")
    print(" 5. 프롬프트 상세 보기")
    print(" 6. 즐겨찾기 추가/해제")
    print(" 7. 즐겨찾기 목록 보기")
    print(" 0. 종료")
    print("=" * 40)


def main():
    prompts = get_default_prompts()

    while True:
        show_menu()
        choice = input("번호를 선택하세요: ").strip()

        if choice == "1":
            add_prompt(prompts)
        elif choice == "2":
            show_list(prompts)
        elif choice == "0":
            print("프로그램을 종료합니다.")
            break
        else:
            print("잘못된 번호입니다. 다시 선택해주세요.")


if __name__ == "__main__":
    main()
```
